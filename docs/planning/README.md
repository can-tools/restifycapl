# Planning documents

Drop project requirements, notes, and decisions here for the `planner`
agent to use as context.

Note: this location is a convention, not an enforced access boundary —
Claude Code's permission rules (`settings.json`) are global across the
session, not scoped per subagent. See `CLAUDE.md` for details.
